@model IEnumerable<ECommerceWeb.Models.DiscountCoupon>

<body> 
    <h3>discount Coupon</h3>
    
    <table class="table table-bordered" cellpadding="2" cellspacing="2">
        <tr>
            <th>option</th>
            <th>Coupon Code</th>
            <th>Valli till</th>
            <th>Discount Price</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @*<a asp-controller="DiscountCoupon" asp-action="DeleteCoupon" asp-route-id="@item.CouponId">*@
                    <button class="btn btn-success" onclick="return validateData('@item.CouponId');">Remove</button>@*</a>*@ /
                    <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#exampleModal" onclick="GetDetails('@item.CouponId')">Edit</button>
                </td>
                <td>@item.CouponCode</td>
                <td>@item.Valid_Till</td>
                <td>@item.Discount</td>
            </tr>
        }
    </table>
    <br>
    <a asp-controller="DiscountCoupon" asp-action="DiscountCoupon"> </a>
       
    <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#exampleModal" data-whatever="" onclick="GetDetails(0)">Add Coupon</button>
   


<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h1 class="modal-title fs-5" id="exampleModalLabel">Add Coupon</h1>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
                <div class="modal-body" id="CouponBody">
      </div>
    </div>
  </div>
</div>
</body>
<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script>
    const exampleModal = document.getElementById('exampleModal')
if (exampleModal) {
  exampleModal.addEventListener('show.bs.modal', event => {
    // Button that triggered the modal
    const button = event.relatedTarget
    // Extract info from data-bs-* attributes\
    // If necessary, you could initiate an Ajax request here
    // and then do the updating in a callback.

    // Update the modal's content.
    const modalTitle = exampleModal.querySelector('.modal-title')

    modalTitle.textContent = `Add Coupon`
  })
}
</script>
<script src="@Url.Content("~/js/DiscountCoupon/DiscountCoupon.js")" type="text/javascript"></script>

